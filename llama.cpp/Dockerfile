# syntax=docker/dockerfile:1
ARG BASE_IMAGE=ghcr.io/ggerganov/llama.cpp:server
FROM ${BASE_IMAGE}

# The base image already sets entrypoint to llama-server
# Default host and port are overridden via command arguments in compose.
